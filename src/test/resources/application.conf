akka {

  loglevel = "DEBUG"
  stdout-loglevel = "DEBUG"
  log-dead-letters-during-shutdown = on
  log-dead-letters = 5

  actor {

    debug {
      receive = on
      autoreceive = off
      lifecycle = off
      fsm = on
    }

    deployment {

      
    }

  }

  test {
    timefactor = 1
  }
}